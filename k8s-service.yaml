apiVersion: v1
kind: Service
metadata:
  name: todo-app-service
  labels:
    app: todo-app
spec:
  type: NodePort
  selector:
    app: todo-app
    version: blue
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
    nodePort: 30080
    name: http
